# MCP protocol
mcp>=1.0.0

# FastMCP framework
fastmcp>=2.0.0

# ASGI stack
starlette>=0.40.0
uvicorn>=0.30.0

# JWT / JWKS verification
python-jose[cryptography]>=3.3.0

# HTTP client (JWKS fetch + DCR proxy)
httpx>=0.27.0

# JWKS TTL cache
cachetools>=5.3.0

# Data validation
pydantic>=2.0.0
